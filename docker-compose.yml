version: '3.8'

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

  server:
    build: .
    restart: always
    ports:
      - '4001:4001'
    depends_on:
      - mongo

volumes:
  mongo-data:
